---
title: Webgen::Output::FileSystem
summary: Writes the generated content to a specified directory
---
## Description

This is the default output extension used when creating a new webgen website. It writes the
generated content to files under a specified directory.

The first parameter for the file system output is the directory under which the generated files
should be written.

## Examples

The used output extension can be specified via the [`output` configuration
option](../references/configuration.html#output), so each of the examples below can be specified in
the `config.yaml` file.

1.  The default configuration: all files are written to the `out` directory of the website directory

        output: [Webgen::Output::FileSystem, out]

2.  Writing files to an absolute directory:

        output: [Webgen::Output::FileSystem, /var/www/documentation]
